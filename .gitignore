# Dependencies
node_modules

# Build output
dist

# Environment — never commit secrets.
# `.env.example` is the committed template; local values live in `.env.local`.
.env
.env*.local

# Editor / OS
.DS_Store
*.swp

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript incremental build cache (generated, tracked accidentally before)
*.tsbuildinfo

# Local audit artifact
audit-prod.json
# Local editor configuration
.vscode/

# Local document test fixtures
aadhaar1.pdf
aadhaar2.pdf

# Supabase local CLI state
supabase/.temp/

